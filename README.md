## A simple interpreter (Educational Purpose)

### Lexical Analysis
A simple lexer for a toy programming language (Lox) from `Crafting Interpreters` book in Java.

## Usage
The project uses Gradle build system.
